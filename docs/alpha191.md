
## **Alpha001 - 量价相关/协同** 

```
(-1*CORR(RANK(DELTA(LOG(VOLUME),1)),RANK(((CLOSE-OPEN)/OPEN)),6))
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取开盘价、收盘价、成交量，再用横截面排序、滚动相关、变化 量把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、6；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha002 - 波动振幅/日内结构** 

```
(-1*DELTA((((CLOSE-LOW)-(HIGH-CLOSE))/(HIGH-LOW)),1))
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取最高价、最低价、收盘价，再用变化量把短期价格/成交 行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**6** 

## **Alpha003 - 动量反转/均值回复** 

```
SUM((CLOSE=DELAY(CLOSE,1)?0:CLOSE-(CLOSE>DELAY(CLOSE,1)?MIN(LOW,DELAY(CLOSE,1)):MAX(HIGH,
DELAY(CLOSE,1)))),6)
```

## **关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取最高价、最低价、收盘价，再用滞后值、条件分支把短期 价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、6；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha004 - 波动振幅/日内结构** 

```
((((SUM(CLOSE,8)/8)+STD(CLOSE,8))<(SUM(CLOSE,2)/2))?(-1*1):(((SUM(CLOSE,2)/2)<((SUM(CLOSE,8)/
8)-STD(CLOSE,8)))?1:(((1<(VOLUME/MEAN(VOLUME,20)))||((VOLUME/MEAN(VOLUME,20))==1))?1:(-1*1)))
)
```

**关键拆解：STD=过去 n 天标准差，衡量波动；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取收盘价、成交量，再用波动率、条件分支把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**8、2、1、20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放** 

**。** 

- **注意分母为 0 或极小值时要做保护。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha005 - 量价相关/协同** 

```
(-1*TSMAX(CORR(TSRANK(VOLUME,5),TSRANK(HIGH,5),5),3))
```

**关键拆解：RANK=当日股票池横截面排序；TSRANK=单只股票在过去 n 天内的历史排名；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最高价、成交量，再用横截面排序、时间序列排名、滚动相关 把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、5、3；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **TSRANK 是单只股票自己的历史排名，不能和 RANK 混用。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

**Alpha 191 因子公式小白学习手册** 

**7** 

## **Alpha006 - 排序位置/相对强弱** 

```
(RANK(SIGN(DELTA((((OPEN*0.85)+(HIGH*0.15))),4)))*-1)
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序** 

**一句话：这是一个偏「排序位置/相对强弱」的因子，主要读取开盘价、最高价，再用横截面排序、变化量把短期价格/ 成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**85、15、4、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

## **Alpha007 - 成交量/资金活跃** 

```
((RANK(MAX((VWAP-CLOSE),3))+RANK(MIN((VWAP-CLOSE),3)))*RANK(DELTA(VOLUME,3)))
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取收盘价、均价/VWAP、成交量，再用横截面排序、变化量 把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**3；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

## **Alpha008 - 成交量/资金活跃** 

```
RANK(DELTA(((((HIGH+LOW)/2)*0.2)+(VWAP*0.8)),4)*-1)
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取最高价、最低价、均价/VWAP，再用横截面排序、变化量 把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**2、8、4、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

**Alpha 191 因子公式小白学习手册** 

**8** 

## **Alpha009 - 波动振幅/日内结构** 

```
SMA(((HIGH+LOW)/2-(DELAY(HIGH,1)+DELAY(LOW,1))/2)*(HIGH-LOW)/VOLUME,7,2)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；SMA=递推平滑均线** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取最高价、最低价、成交量，再用滞后值、平滑均线把短期 价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**2、1、7；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha010 - 波动振幅/日内结构** 

```
(RANK(MAX(((RET<0)?STD(RET,20):CLOSE)^2),5))
```

**关键拆解：RANK=当日股票池横截面排序；STD=过去 n 天标准差，衡量波动；A?B:C=如果 A 成立取 B，否则取 C；^=幂运算，不是异或** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取收盘价、收益率，再用横截面排序、波动率、条件分支、 幂运算把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20、2、5；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha011 - 波动振幅/日内结构** 

```
SUM(((CLOSE-LOW)-(HIGH-CLOSE))./(HIGH-LOW).*VOLUME,6)
```

## **关键拆解：主要是价格和成交量字段的四则组合，重点看变量之间的相对位置。** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取最高价、最低价、收盘价、成交量，再用四则运算把短期 价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**6；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**9** 

## **Alpha012 - 成交量/资金活跃** 

```
(RANK((OPEN-(SUM(VWAP,10)/10))))*(-1*(RANK(ABS((CLOSE-VWAP)))))
```

**关键拆解：RANK=当日股票池横截面排序** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取开盘价、收盘价、均价/VWAP，再用横截面排序把短期价 格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**10、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

## **Alpha013 - 成交量/资金活跃** 

```
(((HIGH*LOW)^0.5)-VWAP)
```

## **关键拆解：^=幂运算，不是异或** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取最高价、最低价、均价/VWAP，再用幂运算把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**5；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **计算后建议做去极值、缺失值处理和行业/市值中性化，再检验 IC 与分层收益。** 

## **Alpha014 - 动量反转/均值回复** 

```
CLOSE-DELAY(CLOSE,5)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值把短期价格/成交行为压缩成一个可 排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**5；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它本质上在看短期收益。收益可以被当作动量，也可以被反向使用为均值回复信号。** 

- **计算后建议做去极值、缺失值处理和行业/市值中性化，再检验 IC 与分层收益。** 

**Alpha 191 因子公式小白学习手册** 

**10** 

## **Alpha015 - 动量反转/均值回复** 

```
OPEN/DELAY(CLOSE,1)-1
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取开盘价、收盘价，再用滞后值把短期价格/成交行为压缩 成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha016 - 量价相关/协同** 

- `(-1*TSMAX(RANK(CORR(RANK(VOLUME),RANK(VWAP),5)),5))` 

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取均价/VWAP、成交量，再用横截面排序、滚动相关把短期价 格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、5；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha017 - 成交量/资金活跃** 

```
RANK((VWAP-MAX(VWAP,15)))^DELTA(CLOSE,5)
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；^=幂运算，不是异或** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取收盘价、均价/VWAP，再用横截面排序、变化量、幂运算 把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**15、5；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

**Alpha 191 因子公式小白学习手册** 

**11** 

## **Alpha018 - 动量反转/均值回复** 

```
CLOSE/DELAY(CLOSE,5)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值把短期价格/成交行为压缩成一个可 排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**5；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它本质上在看短期收益。收益可以被当作动量，也可以被反向使用为均值回复信号。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha019 - 动量反转/均值回复** 

```
(CLOSE<DELAY(CLOSE,5)?(CLOSE-DELAY(CLOSE,5))/DELAY(CLOSE,5):(CLOSE=DELAY(CLOSE,5)?0:(CLOSE-
DELAY(CLOSE,5))/CLOSE))
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、条件分支把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**5；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它本质上在看短期收益。收益可以被当作动量，也可以被反向使用为均值回复信号。** 

- **注意分母为 0 或极小值时要做保护。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha020 - 动量反转/均值回复** 

```
(CLOSE-DELAY(CLOSE,6))/DELAY(CLOSE,6)*100
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值把短期价格/成交行为压缩成一个可 排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**6、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它本质上在看短期收益。收益可以被当作动量，也可以被反向使用为均值回复信号。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**12** 

## **Alpha021 - 市场联动/回归** 

```
REGBETA(MEAN(CLOSE,6),SEQUENCE(6))
```

**关键拆解：REGBETA=回归斜率/敏感度** 

**一句话：这是一个偏「市场联动/回归」的因子，主要读取收盘价，再用回归系数把短期价格/成交行为压缩成一个可排 序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**6；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它把个股和市场或风格因子放在一起比较，试图剥离大盘/风格影响后观察个股自身的短期特征。** 

- **计算后建议做去极值、缺失值处理和行业/市值中性化，再检验 IC 与分层收益。** 

## **Alpha022 - 动量反转/均值回复** 

```
SMEAN(((CLOSE-MEAN(CLOSE,6))/MEAN(CLOSE,6)-DELAY((CLOSE-MEAN(CLOSE,6))/MEAN(CLOSE,6),3)),12,
1)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值把短期价格/成交行为压缩成一个可 排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**6、3、12、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

- **校对提示：原文使用 SMEAN，但附录未定义。实现前需和所用平台口径核对；很多实现会把它当作平滑均值处理。** 

## **Alpha023 - 波动振幅/日内结构** 

```
SMA((CLOSE>DELAY(CLOSE,1)?STD(CLOSE,20):0),20,1)/(SMA((CLOSE>DELAY(CLOSE,1)?STD(CLOSE,20):0),
20,1)+SMA((CLOSE<=DELAY(CLOSE,1)?STD(CLOSE,20):0),20,1))*100
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；STD=过去 n 天标准差，衡量波动；SMA=递推平滑均线；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取收盘价，再用滞后值、平滑均线、波动率、条件分支把短 期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、20、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放** 

**。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

**校对提示：原文/网页中出现 STD(CLOSE:20) 的写法；本手册按附录函数口径整理为 STD(CLOSE,20)。** 

**Alpha 191 因子公式小白学习手册** 

**13** 

## **Alpha024 - 动量反转/均值回复** 

```
SMA(CLOSE-DELAY(CLOSE,5),5,1)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；SMA=递推平滑均线** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、平滑均线把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**5、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha025 - 成交量/资金活跃** 

```
((-1*RANK((DELTA(CLOSE,7)*(1-RANK(DECAYLINEAR((VOLUME/MEAN(VOLUME,20)),9))))))*(1+
RANK(SUM(RET,250))))
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；DECAYLINEAR=越近的数据权重越高** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取收盘价、成交量、收益率，再用横截面排序、变化量、线性 衰减加权把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、7、20、9、250；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha026 - 量价相关/协同** 

```
((((SUM(CLOSE,7)/7)-CLOSE))+((CORR(VWAP,DELAY(CLOSE,5),230))))
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取收盘价、均价/VWAP，再用滚动相关、滞后值把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**7、5、230；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **注意分母为 0 或极小值时要做保护。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

**Alpha 191 因子公式小白学习手册** 

**14** 

## **Alpha027 - 动量反转/均值回复** 

```
WMA((CLOSE-DELAY(CLOSE,3))/DELAY(CLOSE,3)*100+(CLOSE-DELAY(CLOSE,6))/DELAY(CLOSE,6)*100,12)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；WMA=加权移动平均** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、加权均线把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**3、100、6、12；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha028 - 综合价格形态** 

```
3*SMA((CLOSE-TSMIN(LOW,9))/(TSMAX(HIGH,9)-TSMIN(LOW,9))*100,3,1)-2*SMA(SMA((CLOSE-TSMIN(LOW,
9))/(MAX(HIGH,9)-TSMAX(LOW,9))*100,3,1),3,1)
```

## **关键拆解：SMA=递推平滑均线** 

**一句话：这是一个偏「综合价格形态」的因子，主要读取最高价、最低价、收盘价，再用平滑均线把短期价格/成交行为 压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**3、9、100、1、2；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha029 - 成交量/资金活跃** 

```
(CLOSE-DELAY(CLOSE,6))/DELAY(CLOSE,6)*VOLUME
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取收盘价、成交量，再用滞后值把短期价格/成交行为压缩成 一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**6；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它本质上在看短期收益。收益可以被当作动量，也可以被反向使用为均值回复信号。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**15** 

## **Alpha030 - 市场联动/回归** 

```
WMA((REGRESI(CLOSE/DELAY(CLOSE)-1,MKT,SMB,HML,60))^2,20)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；WMA=加权移动平均；REGRESI=回归残差；^=幂运算，不是异或** 

**一句话：这是一个偏「市场联动/回归」的因子，主要读取收盘价，再用滞后值、加权均线、回归残差、幂运算把短期价 格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、60、2、20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha031 - 动量反转/均值回复** 

```
(CLOSE-MEAN(CLOSE,12))/MEAN(CLOSE,12)*100
```

**关键拆解：主要是价格和成交量字段的四则组合，重点看变量之间的相对位置。** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用四则运算把短期价格/成交行为压缩成一个 可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**12、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha032 - 量价相关/协同** 

```
(-1*SUM(RANK(CORR(RANK(HIGH),RANK(VOLUME),3)),3))
```

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最高价、成交量，再用横截面排序、滚动相关把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、3；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

**Alpha 191 因子公式小白学习手册** 

**16** 

## **Alpha033 - 成交量/资金活跃** 

```
((((-1*TSMIN(LOW,5))+DELAY(TSMIN(LOW,5),5))*RANK(((SUM(RET,240)-SUM(RET,20))/220)))*
TSRANK(VOLUME,5))
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；RANK=当日股票池横截面排序；TSRANK=单只股票在过去 n 天内的历史排名** 

- **一句话：这是一个偏「成交量/资金活跃」的因子，主要读取最低价、成交量、收益率，再用横截面排序、时间序列排名 、滞后值把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、5、240、20、220；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **TSRANK 是单只股票自己的历史排名，不能和 RANK 混用。** 

## **Alpha034 - 动量反转/均值回复** 

```
MEAN(CLOSE,12)/CLOSE
```

**关键拆解：主要是价格和成交量字段的四则组合，重点看变量之间的相对位置。** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用四则运算把短期价格/成交行为压缩成一个 可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**12；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha035 - 量价相关/协同** 

```
(MIN(RANK(DECAYLINEAR(DELTA(OPEN,1),15)),RANK(DECAYLINEAR(CORR((VOLUME),((OPEN*0.65)+(OPEN*
0.35)),17),7)))*-1)
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数；DECAYLINEAR=越近的数据权重越高** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取开盘价、成交量，再用横截面排序、滚动相关、变化量、线性 衰减加权把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、15、65、35、17、7；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

**Alpha 191 因子公式小白学习手册** 

**17** 

## **Alpha036 - 量价相关/协同** 

```
RANK(SUM(CORR(RANK(VOLUME),RANK(VWAP)),6),2)
```

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取均价/VWAP、成交量，再用横截面排序、滚动相关把短期价 格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**6、2；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha037 - 排序位置/相对强弱** 

```
(-1*RANK(((SUM(OPEN,5)*SUM(RET,5))-DELAY((SUM(OPEN,5)*SUM(RET,5)),10))))
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；RANK=当日股票池横截面排序** 

**一句话：这是一个偏「排序位置/相对强弱」的因子，主要读取开盘价、收益率，再用横截面排序、滞后值把短期价格/ 成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、5、10；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

## **Alpha038 - 条件统计/规则触发** 

```
(((SUM(HIGH,20)/20)<HIGH)?(-1*DELTA(HIGH,2)):0)
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「条件统计/规则触发」的因子，主要读取最高价，再用变化量、条件分支把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20、1、2；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

**Alpha 191 因子公式小白学习手册** 

**18** 

## **Alpha039 - 量价相关/协同** 

```
((RANK(DECAYLINEAR(DELTA((CLOSE),2),8))-RANK(DECAYLINEAR(CORR(((VWAP*0.3)+(OPEN*0.7)),
SUM(MEAN(VOLUME,180),37),14),12)))*-1)
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；CORR=过去 n** 

**天两个序列的相关系数；DECAYLINEAR=越近的数据权重越高** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取开盘价、收盘价、均价/VWAP、成交量，再用横截面排序、 滚动相关、变化量、线性衰减加权把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为 2、8、3、7、180、37、14、12；再计算公式 里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha040 - 成交量/资金活跃** 

```
SUM((CLOSE>DELAY(CLOSE,1)?VOLUME:0),26)/SUM((CLOSE<=DELAY(CLOSE,1)?VOLUME:0),26)*100
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取收盘价、成交量，再用滞后值、条件分支把短期价格/成交 行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、26、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha041 - 成交量/资金活跃** 

```
(RANK(MAX(DELTA((VWAP),3),5))*-1)
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取均价/VWAP，再用横截面排序、变化量把短期价格/成交行 为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**3、5、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

**Alpha 191 因子公式小白学习手册** 

**19** 

## **Alpha042 - 量价相关/协同** 

```
((-1*RANK(STD(HIGH,10)))*CORR(HIGH,VOLUME,10))
```

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数；STD=过去 n 天标准差，衡量波动** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最高价、成交量，再用横截面排序、滚动相关、波动率把短期 价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、10；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha043 - 成交量/资金活跃** 

```
SUM((CLOSE>DELAY(CLOSE,1)?VOLUME:(CLOSE<DELAY(CLOSE,1)?-VOLUME:0)),6)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取收盘价、成交量，再用滞后值、条件分支把短期价格/成交 行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、6；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha044 - 量价相关/协同** 

```
(TSRANK(DECAYLINEAR(CORR(((LOW)),MEAN(VOLUME,10),7),6),4)+TSRANK(DECAYLINEAR(DELTA((VWAP),3),
10),15))
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；TSRANK=单只股票在过去 n 天内的历史排名；CORR=过去 n 天两个序列的相关系数；DECAYLINEAR=越近的数据权重越高** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最低价、均价/VWAP、成交量，再用横截面排序、时间序列 排名、滚动相关、变化量、线性衰减加权把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**10、7、6、4、3、15；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **TSRANK 是单只股票自己的历史排名，不能和 RANK 混用。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

**Alpha 191 因子公式小白学习手册** 

**20** 

## **Alpha045 - 量价相关/协同** 

```
(RANK(DELTA((((CLOSE*0.6)+(OPEN*0.4))),1))*RANK(CORR(VWAP,MEAN(VOLUME,150),15)))
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取开盘价、收盘价、均价/VWAP、成交量，再用横截面排序、 滚动相关、变化量把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**6、4、1、150、15；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha046 - 动量反转/均值回复** 

```
(MEAN(CLOSE,3)+MEAN(CLOSE,6)+MEAN(CLOSE,12)+MEAN(CLOSE,24))/(4*CLOSE)
```

## **关键拆解：主要是价格和成交量字段的四则组合，重点看变量之间的相对位置。** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用四则运算把短期价格/成交行为压缩成一个 可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**3、6、12、24、4；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha047 - 综合价格形态** 

```
SMA((TSMAX(HIGH,6)-CLOSE)/(TSMAX(HIGH,6)-TSMIN(LOW,6))*100,9,1)
```

## **关键拆解：SMA=递推平滑均线** 

**一句话：这是一个偏「综合价格形态」的因子，主要读取最高价、最低价、收盘价，再用平滑均线把短期价格/成交行为 压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**6、100、9、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

**Alpha 191 因子公式小白学习手册** 

**21** 

## **Alpha048 - 成交量/资金活跃** 

```
(-1*((RANK(((SIGN((CLOSE-DELAY(CLOSE,1)))+SIGN((DELAY(CLOSE,1)-DELAY(CLOSE,2))))+
SIGN((DELAY(CLOSE,2)-DELAY(CLOSE,3))))))*SUM(VOLUME,5))/SUM(VOLUME,20))
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；RANK=当日股票池横截面排序** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取收盘价、成交量，再用横截面排序、滞后值把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、2、3、5、20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

## **Alpha049 - 条件统计/规则触发** 

```
SUM(((HIGH+LOW)>=(DELAY(HIGH,1)+DELAY(LOW,1))?0:MAX(ABS(HIGH-DELAY(HIGH,1)),ABS(LOW-
DELAY(LOW,1)))),12)/(SUM(((HIGH+LOW)>=(DELAY(HIGH,1)+DELAY(LOW,1))?0:MAX(ABS(HIGH-DELAY(HIGH,
1)),ABS(LOW-DELAY(LOW,1)))),12)+SUM(((HIGH+LOW)<=(DELAY(HIGH,1)+DELAY(LOW,1))?0:MAX(ABS(HIGH-
DELAY(HIGH,1)),ABS(LOW-DELAY(LOW,1)))),12))
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「条件统计/规则触发」的因子，主要读取最高价、最低价，再用滞后值、条件分支把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、12；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha050 - 条件统计/规则触发** 

```
SUM(((HIGH+LOW)<=(DELAY(HIGH,1)+DELAY(LOW,1))?0:MAX(ABS(HIGH-DELAY(HIGH,1)),ABS(LOW-
DELAY(LOW,1)))),12)/(SUM(((HIGH+LOW)<=(DELAY(HIGH,1)+DELAY(LOW,1))?0:MAX(ABS(HIGH-DELAY(HIGH,
1)),ABS(LOW-DELAY(LOW,1)))),12)+SUM(((HIGH+LOW)>=(DELAY(HIGH,1)+DELAY(LOW,1))?0:MAX(ABS(HIGH-
DELAY(HIGH,1)),ABS(LOW-DELAY(LOW,1)))),12))-SUM(((HIGH+LOW)>=(DELAY(HIGH,1)+DELAY(LOW,1))?0:
MAX(ABS(HIGH-DELAY(HIGH,1)),ABS(LOW-DELAY(LOW,1)))),12)/(SUM(((HIGH+LOW)>=(DELAY(HIGH,1)+
DELAY(LOW,1))?0:MAX(ABS(HIGH-DELAY(HIGH,1)),ABS(LOW-DELAY(LOW,1)))),12)+SUM(((HIGH+LOW)
<=(DELAY(HIGH,1)+DELAY(LOW,1))?0:MAX(ABS(HIGH-DELAY(HIGH,1)),ABS(LOW-DELAY(LOW,1)))),12))
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「条件统计/规则触发」的因子，主要读取最高价、最低价，再用滞后值、条件分支把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、12；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

**Alpha 191 因子公式小白学习手册** 

**22** 

## **Alpha051 - 条件统计/规则触发** 

```
SUM(((HIGH+LOW)<=(DELAY(HIGH,1)+DELAY(LOW,1))?0:MAX(ABS(HIGH-DELAY(HIGH,1)),ABS(LOW-
DELAY(LOW,1)))),12)/(SUM(((HIGH+LOW)<=(DELAY(HIGH,1)+DELAY(LOW,1))?0:MAX(ABS(HIGH-DELAY(HIGH,
1)),ABS(LOW-DELAY(LOW,1)))),12)+SUM(((HIGH+LOW)>=(DELAY(HIGH,1)+DELAY(LOW,1))?0:MAX(ABS(HIGH-
DELAY(HIGH,1)),ABS(LOW-DELAY(LOW,1)))),12))
```

## **关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「条件统计/规则触发」的因子，主要读取最高价、最低价，再用滞后值、条件分支把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、12；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha052 - 综合价格形态** 

```
SUM(MAX(0,HIGH-DELAY((HIGH+LOW+CLOSE)/3,1)),26)/SUM(MAX(0,DELAY((HIGH+LOW+CLOSE)/3,1)-LOW),
26)*100
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较** 

**一句话：这是一个偏「综合价格形态」的因子，主要读取最高价、最低价、收盘价，再用滞后值把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**3、1、26、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

**校对提示：原文/网页分母中有 -L，本手册按上下文整理为 -LOW。** 

## **Alpha053 - 动量反转/均值回复** 

```
COUNT(CLOSE>DELAY(CLOSE,1),12)/12*100
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；COUNT=统计条件成立的次数** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、条件计数把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、12、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它像是在数事件：过去若干天里，某种上涨、下跌、放量或同向条件出现了多少次。频率本身就是信号。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**23** 

## **Alpha054 - 量价相关/协同** 

```
(-1*RANK((STD(ABS(CLOSE-OPEN))+(CLOSE-OPEN))+CORR(CLOSE,OPEN,10)))
```

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数；STD=过去 n 天标准差，衡量波动** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取开盘价、收盘价，再用横截面排序、滚动相关、波动率把短期 价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、10；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha055 - 动量反转/均值回复** 

```
SUM(16*(CLOSE-DELAY(CLOSE,1)+(CLOSE-OPEN)/2+DELAY(CLOSE,1)-DELAY(OPEN,1))/((ABS(HIGH-
DELAY(CLOSE,1))>ABS(LOW-DELAY(CLOSE,1))&ABS(HIGH-DELAY(CLOSE,1))>ABS(HIGH-DELAY(LOW,1))?
ABS(HIGH-DELAY(CLOSE,1))+ABS(LOW-DELAY(CLOSE,1))/2+ABS(DELAY(CLOSE,1)-DELAY(OPEN,1))/4:
(ABS(LOW-DELAY(CLOSE,1))>ABS(HIGH-DELAY(LOW,1))&ABS(LOW-DELAY(CLOSE,1))>ABS(HIGH-DELAY(CLOSE,
1))?ABS(LOW-DELAY(CLOSE,1))+ABS(HIGH-DELAY(CLOSE,1))/2+ABS(DELAY(CLOSE,1)-DELAY(OPEN,1))/4:
ABS(HIGH-DELAY(LOW,1))+ABS(DELAY(CLOSE,1)-DELAY(OPEN,1))/4)))*MAX(ABS(HIGH-DELAY(CLOSE,1)),
ABS(LOW-DELAY(CLOSE,1))),20)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取开盘价、最高价、最低价、收盘价，再用滞后值、条件分 支把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**16、1、2、4、20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它本质上在看短期收益。收益可以被当作动量，也可以被反向使用为均值回复信号。** 

- **注意分母为 0 或极小值时要做保护。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

**Alpha 191 因子公式小白学习手册** 

**24** 

## **Alpha056 - 量价相关/协同** 

```
(RANK((OPEN-TSMIN(OPEN,12)))<RANK((RANK(CORR(SUM(((HIGH+LOW)/2),19),SUM(MEAN(VOLUME,40),19),
13))^5)))
```

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数；^=幂运算，不是异或** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取开盘价、最高价、最低价、成交量，再用横截面排序、滚动相 关、幂运算把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**12、2、19、40、13、5；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha057 - 综合价格形态** 

```
SMA((CLOSE-TSMIN(LOW,9))/(TSMAX(HIGH,9)-TSMIN(LOW,9))*100,3,1)
```

**关键拆解：SMA=递推平滑均线** 

**一句话：这是一个偏「综合价格形态」的因子，主要读取最高价、最低价、收盘价，再用平滑均线把短期价格/成交行为 压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**9、100、3、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha058 - 动量反转/均值回复** 

```
COUNT(CLOSE>DELAY(CLOSE,1),20)/20*100
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；COUNT=统计条件成立的次数** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、条件计数把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、20、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它像是在数事件：过去若干天里，某种上涨、下跌、放量或同向条件出现了多少次。频率本身就是信号。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**25** 

## **Alpha059 - 动量反转/均值回复** 

```
SUM((CLOSE=DELAY(CLOSE,1)?0:CLOSE-(CLOSE>DELAY(CLOSE,1)?MIN(LOW,DELAY(CLOSE,1)):MAX(HIGH,
DELAY(CLOSE,1)))),20)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取最高价、最低价、收盘价，再用滞后值、条件分支把短期 价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha060 - 波动振幅/日内结构** 

```
SUM(((CLOSE-LOW)-(HIGH-CLOSE))./(HIGH-LOW).*VOLUME,20)
```

**关键拆解：主要是价格和成交量字段的四则组合，重点看变量之间的相对位置。** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取最高价、最低价、收盘价、成交量，再用四则运算把短期 价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放** 

**。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha061 - 量价相关/协同** 

```
(MAX(RANK(DECAYLINEAR(DELTA(VWAP,1),12)),RANK(DECAYLINEAR(RANK(CORR((LOW),MEAN(VOLUME,80),8))
,17)))*-1)
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数；DECAYLINEAR=越近的数据权重越高** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最低价、均价/VWAP、成交量，再用横截面排序、滚动相关 、变化量、线性衰减加权把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、12、80、8、17；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

**Alpha 191 因子公式小白学习手册** 

**26** 

## **Alpha062 - 量价相关/协同** 

```
(-1*CORR(HIGH,RANK(VOLUME),5))
```

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最高价、成交量，再用横截面排序、滚动相关把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、5；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha063 - 动量反转/均值回复** 

```
SMA(MAX(CLOSE-DELAY(CLOSE,1),0),6,1)/SMA(ABS(CLOSE-DELAY(CLOSE,1)),6,1)*100
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；SMA=递推平滑均线** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、平滑均线把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、6、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha064 - 量价相关/协同** 

```
(MAX(RANK(DECAYLINEAR(CORR(RANK(VWAP),RANK(VOLUME),4),4)),
RANK(DECAYLINEAR(MAX(CORR(RANK(CLOSE),RANK(MEAN(VOLUME,60)),4),13),14)))*-1)
```

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数；DECAYLINEAR=越近的数据权重越高** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取收盘价、均价/VWAP、成交量，再用横截面排序、滚动相关 、线性衰减加权把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **4、60、13、14、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

**Alpha 191 因子公式小白学习手册** 

**27** 

## **Alpha065 - 动量反转/均值回复** 

```
MEAN(CLOSE,6)/CLOSE
```

## **关键拆解：主要是价格和成交量字段的四则组合，重点看变量之间的相对位置。** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用四则运算把短期价格/成交行为压缩成一个 可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**6；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha066 - 动量反转/均值回复** 

```
(CLOSE-MEAN(CLOSE,6))/MEAN(CLOSE,6)*100
```

## **关键拆解：主要是价格和成交量字段的四则组合，重点看变量之间的相对位置。** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用四则运算把短期价格/成交行为压缩成一个 可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**6、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha067 - 动量反转/均值回复** 

```
SMA(MAX(CLOSE-DELAY(CLOSE,1),0),24,1)/SMA(ABS(CLOSE-DELAY(CLOSE,1)),24,1)*100
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；SMA=递推平滑均线** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、平滑均线把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、24、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

**Alpha 191 因子公式小白学习手册** 

**28** 

## **Alpha068 - 波动振幅/日内结构** 

```
SMA(((HIGH+LOW)/2-(DELAY(HIGH,1)+DELAY(LOW,1))/2)*(HIGH-LOW)/VOLUME,15,2)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；SMA=递推平滑均线** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取最高价、最低价、成交量，再用滞后值、平滑均线把短期 价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**2、1、15；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha069 - 波动振幅/日内结构** 

```
(SUM(DTM,20)>SUM(DBM,20)?(SUM(DTM,20)-SUM(DBM,20))/SUM(DTM,20):(SUM(DTM,20)=SUM(DBM,20)?0:
(SUM(DTM,20)-SUM(DBM,20))/SUM(DBM,20)))
```

**关键拆解：A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取多空力道** 

**DTM/DBM，再用条件分支把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为 20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha070 - 波动振幅/日内结构** 

```
STD(AMOUNT,6)
```

**关键拆解：STD=过去 n 天标准差，衡量波动** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取成交额，再用波动率把短期价格/成交行为压缩成一个可 排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为 6；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **计算后建议做去极值、缺失值处理和行业/市值中性化，再检验 IC 与分层收益。** 

**Alpha 191 因子公式小白学习手册** 

**29** 

## **Alpha071 - 动量反转/均值回复** 

```
(CLOSE-MEAN(CLOSE,24))/MEAN(CLOSE,24)*100
```

## **关键拆解：主要是价格和成交量字段的四则组合，重点看变量之间的相对位置。** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用四则运算把短期价格/成交行为压缩成一个 可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**24、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha072 - 综合价格形态** 

```
SMA((TSMAX(HIGH,6)-CLOSE)/(TSMAX(HIGH,6)-TSMIN(LOW,6))*100,15,1)
```

**关键拆解：SMA=递推平滑均线** 

**一句话：这是一个偏「综合价格形态」的因子，主要读取最高价、最低价、收盘价，再用平滑均线把短期价格/成交行为 压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**6、100、15、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha073 - 量价相关/协同** 

```
((TSRANK(DECAYLINEAR(DECAYLINEAR(CORR((CLOSE),VOLUME,10),16),4),5)-
RANK(DECAYLINEAR(CORR(VWAP,MEAN(VOLUME,30),4),3)))*-1)
```

**关键拆解：RANK=当日股票池横截面排序；TSRANK=单只股票在过去 n 天内的历史排名；CORR=过去 n 天两个序列的相关系数；DECAYLINEAR=越近的数据权重越高** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取收盘价、均价/VWAP、成交量，再用横截面排序、时间序列 排名、滚动相关、线性衰减加权把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为 10、16、4、5、30、3、1；再计算公式里的变 化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **TSRANK 是单只股票自己的历史排名，不能和 RANK 混用。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

**Alpha 191 因子公式小白学习手册** 

**30** 

## **Alpha074 - 量价相关/协同** 

```
(RANK(CORR(SUM(((LOW*0.35)+(VWAP*0.65)),20),SUM(MEAN(VOLUME,40),20),7))+RANK(CORR(RANK(VWAP),
RANK(VOLUME),6)))
```

## **关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最低价、均价/VWAP、成交量，再用横截面排序、滚动相关 把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**35、65、20、40、7、6；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha075 - 市场联动/回归** 

```
COUNT(CLOSE>OPEN&BANCHMARKINDEXCLOSE<BANCHMARKINDEXOPEN,50)/
COUNT(BANCHMARKINDEXCLOSE<BANCHMARKINDEXOPEN,50)
```

**关键拆解：COUNT=统计条件成立的次数** 

**一句话：这是一个偏「市场联动/回归」的因子，主要读取开盘价、收盘价、基准指数，再用条件计数把短期价格/成交 行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**50；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它像是在数事件：过去若干天里，某种上涨、下跌、放量或同向条件出现了多少次。频率本身就是信号。** 

- **注意分母为 0 或极小值时要做保护。** 

- **校对提示：BANCHMARK 是报告原文拼法，含义为 BENCHMARK，即基准指数。** 

## **Alpha076 - 波动振幅/日内结构** 

```
STD(ABS((CLOSE/DELAY(CLOSE,1)-1))/VOLUME,20)/MEAN(ABS((CLOSE/DELAY(CLOSE,1)-1))/VOLUME,20)
```

## **关键拆解：DELAY=取 n 日前的值，用来和过去比较；STD=过去 n 天标准差，衡量波动** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取收盘价、成交量，再用滞后值、波动率把短期价格/成交 行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**31** 

## **Alpha077 - 量价相关/协同** 

```
MIN(RANK(DECAYLINEAR(((((HIGH+LOW)/2)+HIGH)-(VWAP+HIGH)),20)),RANK(DECAYLINEAR(CORR(((HIGH+
LOW)/2),MEAN(VOLUME,40),3),6)))
```

## **关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数；DECAYLINEAR=越近的数据权重越高** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最高价、最低价、均价/VWAP、成交量，再用横截面排序、 滚动相关、线性衰减加权把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**2、20、40、3、6；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha078 - 综合价格形态** 

```
((HIGH+LOW+CLOSE)/3-MA((HIGH+LOW+CLOSE)/3,12))/(0.015*MEAN(ABS(CLOSE-MEAN((HIGH+LOW+CLOSE)/3,
12)),12))
```

## **关键拆解：主要是价格和成交量字段的四则组合，重点看变量之间的相对位置。** 

**一句话：这是一个偏「综合价格形态」的因子，主要读取最高价、最低价、收盘价，再用四则运算把短期价格/成交行为 压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**3、12；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha079 - 动量反转/均值回复** 

```
SMA(MAX(CLOSE-DELAY(CLOSE,1),0),12,1)/SMA(ABS(CLOSE-DELAY(CLOSE,1)),12,1)*100
```

## **关键拆解：DELAY=取 n 日前的值，用来和过去比较；SMA=递推平滑均线** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、平滑均线把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、12、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

**Alpha 191 因子公式小白学习手册** 

**32** 

## **Alpha080 - 成交量/资金活跃** 

```
(VOLUME-DELAY(VOLUME,5))/DELAY(VOLUME,5)*100
```

## **关键拆解：DELAY=取 n 日前的值，用来和过去比较** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取成交量，再用滞后值把短期价格/成交行为压缩成一个可排 序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**5、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha081 - 成交量/资金活跃** 

```
SMA(VOLUME,21,2)
```

## **关键拆解：SMA=递推平滑均线** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取成交量，再用平滑均线把短期价格/成交行为压缩成一个可 排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**21、2；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

- **新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha082 - 综合价格形态** 

```
SMA((TSMAX(HIGH,6)-CLOSE)/(TSMAX(HIGH,6)-TSMIN(LOW,6))*100,20,1)
```

**关键拆解：SMA=递推平滑均线** 

**一句话：这是一个偏「综合价格形态」的因子，主要读取最高价、最低价、收盘价，再用平滑均线把短期价格/成交行为 压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**6、100、20、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

**Alpha 191 因子公式小白学习手册** 

**33** 

## **Alpha083 - 量价相关/协同** 

```
(-1*RANK(COVIANCE(RANK(HIGH),RANK(VOLUME),5)))
```

**关键拆解：RANK=当日股票池横截面排序；COVIANCE=报告原文拼法，表示协方差** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最高价、成交量，再用横截面排序、滚动协方差把短期价格/ 成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、5；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

**校对提示：COVIANCE 是报告原文拼法，含义为 COVARIANCE，即协方差。** 

## **Alpha084 - 成交量/资金活跃** 

```
SUM((CLOSE>DELAY(CLOSE,1)?VOLUME:(CLOSE<DELAY(CLOSE,1)?-VOLUME:0)),20)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取收盘价、成交量，再用滞后值、条件分支把短期价格/成交 行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha085 - 成交量/资金活跃** 

```
(TSRANK((VOLUME/MEAN(VOLUME,20)),20)*TSRANK((-1*DELTA(CLOSE,7)),8))
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；TSRANK=单只股票在过去 n 天内的历史排名** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取收盘价、成交量，再用横截面排序、时间序列排名、变化量 把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20、1、7、8；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **TSRANK 是单只股票自己的历史排名，不能和 RANK 混用。** 

**Alpha 191 因子公式小白学习手册** 

**34** 

## **Alpha086 - 动量反转/均值回复** 

```
((0.25<(((DELAY(CLOSE,20)-DELAY(CLOSE,10))/10)-((DELAY(CLOSE,10)-CLOSE)/10)))?(-1*1):
(((((DELAY(CLOSE,20)-DELAY(CLOSE,10))/10)-((DELAY(CLOSE,10)-CLOSE)/10))<0)?1:((-1*1)*(CLOSE-
DELAY(CLOSE,1)))))
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、条件分支把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**25、20、10、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha087 - 成交量/资金活跃** 

```
((RANK(DECAYLINEAR(DELTA(VWAP,4),7))+TSRANK(DECAYLINEAR(((((LOW*0.9)+(LOW*0.1))-VWAP)/(OPEN-
((HIGH+LOW)/2))),11),7))*-1)
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；TSRANK=单只股票在过去 n 天内的历史排名；DECAYLINEAR=越近的数据权重越高** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取开盘价、最高价、最低价、均价/VWAP，再用横截面排序 、时间序列排名、变化量、线性衰减加权把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**4、7、9、1、2、11；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **TSRANK 是单只股票自己的历史排名，不能和 RANK 混用。** 

## **Alpha088 - 动量反转/均值回复** 

```
(CLOSE-DELAY(CLOSE,20))/DELAY(CLOSE,20)*100
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值把短期价格/成交行为压缩成一个可 排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它本质上在看短期收益。收益可以被当作动量，也可以被反向使用为均值回复信号。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**35** 

## **Alpha089 - 动量反转/均值回复** 

```
2*(SMA(CLOSE,13,2)-SMA(CLOSE,27,2)-SMA(SMA(CLOSE,13,2)-SMA(CLOSE,27,2),10,2))
```

**关键拆解：SMA=递推平滑均线** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用平滑均线把短期价格/成交行为压缩成一个 可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**2、13、27、10；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha090 - 量价相关/协同** 

```
(RANK(CORR(RANK(VWAP),RANK(VOLUME),5))*-1)
```

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取均价/VWAP、成交量，再用横截面排序、滚动相关把短期价 格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**5、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha091 - 量价相关/协同** 

```
((RANK((CLOSE-MAX(CLOSE,5)))*RANK(CORR((MEAN(VOLUME,40)),LOW,5)))*-1)
```

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最低价、收盘价、成交量，再用横截面排序、滚动相关把短期 价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**5、40、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

**Alpha 191 因子公式小白学习手册** 

**36** 

## **Alpha092 - 量价相关/协同** 

```
(MAX(RANK(DECAYLINEAR(DELTA(((CLOSE*0.35)+(VWAP*0.65)),2),3)),
TSRANK(DECAYLINEAR(ABS(CORR((MEAN(VOLUME,180)),CLOSE,13)),5),15))*-1)
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；TSRANK=单只股票在过去 n 天内的历史排名；CORR=过去 n 天两个序列的相关系数；DECAYLINEAR=越近的数据权重越高** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取收盘价、均价/VWAP、成交量，再用横截面排序、时间序列 排名、滚动相关、变化量、线性衰减加权把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为 35、65、2、3、180、13、5、15；再计算公式 里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **TSRANK 是单只股票自己的历史排名，不能和 RANK 混用。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha093 - 条件统计/规则触发** 

```
SUM((OPEN>=DELAY(OPEN,1)?0:MAX((OPEN-LOW),(OPEN-DELAY(OPEN,1)))),20)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「条件统计/规则触发」的因子，主要读取开盘价、最低价，再用滞后值、条件分支把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha094 - 成交量/资金活跃** 

```
SUM((CLOSE>DELAY(CLOSE,1)?VOLUME:(CLOSE<DELAY(CLOSE,1)?-VOLUME:0)),30)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取收盘价、成交量，再用滞后值、条件分支把短期价格/成交 行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、30；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

**Alpha 191 因子公式小白学习手册** 

**37** 

## **Alpha095 - 波动振幅/日内结构** 

```
STD(AMOUNT,20)
```

**关键拆解：STD=过去 n 天标准差，衡量波动** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取成交额，再用波动率把短期价格/成交行为压缩成一个可 排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **计算后建议做去极值、缺失值处理和行业/市值中性化，再检验 IC 与分层收益。** 

## **Alpha096 - 综合价格形态** 

```
SMA(SMA((CLOSE-TSMIN(LOW,9))/(TSMAX(HIGH,9)-TSMIN(LOW,9))*100,3,1),3,1)
```

**关键拆解：SMA=递推平滑均线** 

**一句话：这是一个偏「综合价格形态」的因子，主要读取最高价、最低价、收盘价，再用平滑均线把短期价格/成交行为 压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**9、100、3、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha097 - 波动振幅/日内结构** 

```
STD(VOLUME,10)
```

**关键拆解：STD=过去 n 天标准差，衡量波动** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取成交量，再用波动率把短期价格/成交行为压缩成一个可 排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**10；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **计算后建议做去极值、缺失值处理和行业/市值中性化，再检验 IC 与分层收益。** 

**Alpha 191 因子公式小白学习手册** 

**38** 

## **Alpha098 - 动量反转/均值回复** 

```
((((DELTA((SUM(CLOSE,100)/100),100)/DELAY(CLOSE,100))<0.05)||((DELTA((SUM(CLOSE,100)/100),
100)/DELAY(CLOSE,100))==0.05))?(-1*(CLOSE-TSMIN(CLOSE,100))):(-1*DELTA(CLOSE,3)))
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；DELTA=当前值减 n 日前值，表示变化量；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用变化量、滞后值、条件分支把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**100、1、3；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha099 - 量价相关/协同** 

```
(-1*RANK(COVIANCE(RANK(CLOSE),RANK(VOLUME),5)))
```

**关键拆解：RANK=当日股票池横截面排序；COVIANCE=报告原文拼法，表示协方差** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取收盘价、成交量，再用横截面排序、滚动协方差把短期价格/ 成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、5；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

**校对提示：COVIANCE 是报告原文拼法，含义为 COVARIANCE，即协方差。** 

## **Alpha100 - 波动振幅/日内结构** 

```
STD(VOLUME,20)
```

**关键拆解：STD=过去 n 天标准差，衡量波动** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取成交量，再用波动率把短期价格/成交行为压缩成一个可 排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **计算后建议做去极值、缺失值处理和行业/市值中性化，再检验 IC 与分层收益。** 

**Alpha 191 因子公式小白学习手册** 

**39** 

## **Alpha101 - 量价相关/协同** 

```
((RANK(CORR(CLOSE,SUM(MEAN(VOLUME,30),37),15))<RANK(CORR(RANK(((HIGH*0.1)+(VWAP*0.9))),
RANK(VOLUME),11)))*-1)
```

## **关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最高价、收盘价、均价/VWAP、成交量，再用横截面排序、 滚动相关把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**30、37、15、1、9、11；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha102 - 成交量/资金活跃** 

```
SMA(MAX(VOLUME-DELAY(VOLUME,1),0),6,1)/SMA(ABS(VOLUME-DELAY(VOLUME,1)),6,1)*100
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；SMA=递推平滑均线** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取成交量，再用滞后值、平滑均线把短期价格/成交行为压缩 成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、6、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha103 - 排序位置/相对强弱** 

```
((20-LOWDAY(LOW,20))/20)*100
```

**关键拆解：LOWDAY=最低值距离今天几天** 

**一句话：这是一个偏「排序位置/相对强弱」的因子，主要读取最低价，再用四则运算把短期价格/成交行为压缩成一个 可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

- **新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**40** 

## **Alpha104 - 量价相关/协同** 

```
(-1*(DELTA(CORR(HIGH,VOLUME,5),5)*RANK(STD(CLOSE,20))))
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数；STD=过去 n 天标准差，衡量波动** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最高价、收盘价、成交量，再用横截面排序、滚动相关、变化 量、波动率把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、5、20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha105 - 量价相关/协同** 

```
(-1*CORR(RANK(OPEN),RANK(VOLUME),10))
```

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取开盘价、成交量，再用横截面排序、滚动相关把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、10；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha106 - 动量反转/均值回复** 

```
CLOSE-DELAY(CLOSE,20)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值把短期价格/成交行为压缩成一个可 排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它本质上在看短期收益。收益可以被当作动量，也可以被反向使用为均值回复信号。** 

- **计算后建议做去极值、缺失值处理和行业/市值中性化，再检验 IC 与分层收益。** 

**Alpha 191 因子公式小白学习手册** 

**41** 

## **Alpha107 - 排序位置/相对强弱** 

```
(((-1*RANK((OPEN-DELAY(HIGH,1))))*RANK((OPEN-DELAY(CLOSE,1))))*RANK((OPEN-DELAY(LOW,1))))
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；RANK=当日股票池横截面排序** 

**一句话：这是一个偏「排序位置/相对强弱」的因子，主要读取开盘价、最高价、最低价、收盘价，再用横截面排序、滞 后值把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

## **Alpha108 - 量价相关/协同** 

```
((RANK((HIGH-MIN(HIGH,2)))^RANK(CORR((VWAP),(MEAN(VOLUME,120)),6)))*-1)
```

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数；^=幂运算，不是异或** 

- **一句话：这是一个偏「量价相关/协同」的因子，主要读取最高价、均价/VWAP、成交量，再用横截面排序、滚动相关 、幂运算把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **2、120、6、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha109 - 波动振幅/日内结构** 

```
SMA(HIGH-LOW,10,2)/SMA(SMA(HIGH-LOW,10,2),10,2)
```

**关键拆解：SMA=递推平滑均线** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取最高价、最低价，再用平滑均线把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**10、2；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

**Alpha 191 因子公式小白学习手册** 

**42** 

## **Alpha110 - 动量反转/均值回复** 

```
SUM(MAX(0,HIGH-DELAY(CLOSE,1)),20)/SUM(MAX(0,DELAY(CLOSE,1)-LOW),20)*100
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取最高价、最低价、收盘价，再用滞后值把短期价格/成交 行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、20、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha111 - 波动振幅/日内结构** 

```
SMA(VOL*((CLOSE-LOW)-(HIGH-CLOSE))/(HIGH-LOW),11,2)-SMA(VOL*((CLOSE-LOW)-(HIGH-CLOSE))/(HIGH-
LOW),4,2)
```

## **关键拆解：SMA=递推平滑均线** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取最高价、最低价、收盘价、成交量，再用平滑均线把短期 价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**11、2、4；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放** 

**。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha112 - 动量反转/均值回复** 

```
(SUM((CLOSE-DELAY(CLOSE,1)>0?CLOSE-DELAY(CLOSE,1):0),12)-SUM((CLOSE-DELAY(CLOSE,1)<0?
ABS(CLOSE-DELAY(CLOSE,1)):0),12))/(SUM((CLOSE-DELAY(CLOSE,1)>0?CLOSE-DELAY(CLOSE,1):0),12)+
SUM((CLOSE-DELAY(CLOSE,1)<0?ABS(CLOSE-DELAY(CLOSE,1)):0),12))*100
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、条件分支把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、12、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它本质上在看短期收益。收益可以被当作动量，也可以被反向使用为均值回复信号。** 

- **注意分母为 0 或极小值时要做保护。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

**Alpha 191 因子公式小白学习手册** 

**43** 

## **Alpha113 - 量价相关/协同** 

```
(-1*((RANK((SUM(DELAY(CLOSE,5),20)/20))*CORR(CLOSE,VOLUME,2))*RANK(CORR(SUM(CLOSE,5),
SUM(CLOSE,20),2))))
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取收盘价、成交量，再用横截面排序、滚动相关、滞后值把短期 价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、5、20、2；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha114 - 波动振幅/日内结构** 

```
((RANK(DELAY(((HIGH-LOW)/(SUM(CLOSE,5)/5)),2))*RANK(RANK(VOLUME)))/(((HIGH-LOW)/(SUM(CLOSE,5)
/5))/(VWAP-CLOSE)))
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；RANK=当日股票池横截面排序** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取最高价、最低价、收盘价、均价/VWAP、成交量，再用 横截面排序、滞后值把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **5、2；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

**Alpha 191 因子公式小白学习手册** 

**44** 

## **Alpha115 - 量价相关/协同** 

```
(RANK(CORR(((HIGH*0.9)+(CLOSE*0.1)),MEAN(VOLUME,30),10))^RANK(CORR(TSRANK(((HIGH+LOW)/2),4),
TSRANK(VOLUME,10),7)))
```

**关键拆解：RANK=当日股票池横截面排序；TSRANK=单只股票在过去 n 天内的历史排名；CORR=过去 n 天两个序列的相关系数；^=幂运算，不是异或** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最高价、最低价、收盘价、成交量，再用横截面排序、时间序 列排名、滚动相关、幂运算把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为 9、1、30、10、2、4、7；再计算公式里的变化 、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **TSRANK 是单只股票自己的历史排名，不能和 RANK 混用。** 

## **Alpha116 - 市场联动/回归** 

```
REGBETA(CLOSE,SEQUENCE,20)
```

**关键拆解：REGBETA=回归斜率/敏感度** 

**一句话：这是一个偏「市场联动/回归」的因子，主要读取收盘价，再用回归系数把短期价格/成交行为压缩成一个可排 序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它把个股和市场或风格因子放在一起比较，试图剥离大盘/风格影响后观察个股自身的短期特征。** 

- **计算后建议做去极值、缺失值处理和行业/市值中性化，再检验 IC 与分层收益。** 

## **Alpha117 - 成交量/资金活跃** 

```
((TSRANK(VOLUME,32)*(1-TSRANK(((CLOSE+HIGH)-LOW),16)))*(1-TSRANK(RET,32)))
```

## **关键拆解：RANK=当日股票池横截面排序；TSRANK=单只股票在过去 n 天内的历史排名** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取最高价、最低价、收盘价、成交量、收益率，再用横截面排 序、时间序列排名把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**32、1、16；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **TSRANK 是单只股票自己的历史排名，不能和 RANK 混用。** 

**Alpha 191 因子公式小白学习手册** 

**45** 

## **Alpha118 - 综合价格形态** 

```
SUM(HIGH-OPEN,20)/SUM(OPEN-LOW,20)*100
```

**关键拆解：主要是价格和成交量字段的四则组合，重点看变量之间的相对位置。** 

**一句话：这是一个偏「综合价格形态」的因子，主要读取开盘价、最高价、最低价，再用四则运算把短期价格/成交行为 压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha119 - 量价相关/协同** 

```
(RANK(DECAYLINEAR(CORR(VWAP,SUM(MEAN(VOLUME,5),26),5),7))-
RANK(DECAYLINEAR(TSRANK(MIN(CORR(RANK(OPEN),RANK(MEAN(VOLUME,15)),21),9),7),8)))
```

**关键拆解：RANK=当日股票池横截面排序；TSRANK=单只股票在过去 n 天内的历史排名；CORR=过去 n 天两个序列的相关系数；DECAYLINEAR=越近的数据权重越高** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取开盘价、均价/VWAP、成交量，再用横截面排序、时间序列 排名、滚动相关、线性衰减加权把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为 5、26、7、15、21、9、8；再计算公式里的变 化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **TSRANK 是单只股票自己的历史排名，不能和 RANK 混用。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha120 - 成交量/资金活跃** 

```
(RANK((VWAP-CLOSE))/RANK((VWAP+CLOSE)))
```

**关键拆解：RANK=当日股票池横截面排序** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取收盘价、均价/VWAP，再用横截面排序把短期价格/成交行 为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**无固定窗口；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

**Alpha 191 因子公式小白学习手册** 

**46** 

## **Alpha121 - 量价相关/协同** 

```
((RANK((VWAP-MIN(VWAP,12)))^TSRANK(CORR(TSRANK(VWAP,20),TSRANK(MEAN(VOLUME,60),2),18),3))*-1)
```

**关键拆解：RANK=当日股票池横截面排序；TSRANK=单只股票在过去 n 天内的历史排名；CORR=过去 n 天两个序列的相关系数；^=幂运算，不是异或** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取均价/VWAP、成交量，再用横截面排序、时间序列排名、滚 动相关、幂运算把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为 12、20、60、2、18、3、1；再计算公式里的 变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **TSRANK 是单只股票自己的历史排名，不能和 RANK 混用。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha122 - 综合价格形态** 

```
(SMA(SMA(SMA(LOG(CLOSE),13,2),13,2),13,2)-DELAY(SMA(SMA(SMA(LOG(CLOSE),13,2),13,2),13,2),1))/
DELAY(SMA(SMA(SMA(LOG(CLOSE),13,2),13,2),13,2),1)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；SMA=递推平滑均线** 

**一句话：这是一个偏「综合价格形态」的因子，主要读取收盘价，再用滞后值、平滑均线把短期价格/成交行为压缩成一 个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**13、2、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

**Alpha 191 因子公式小白学习手册** 

**47** 

## **Alpha123 - 量价相关/协同** 

```
((RANK(CORR(SUM(((HIGH+LOW)/2),20),SUM(MEAN(VOLUME,60),20),9))<RANK(CORR(LOW,VOLUME,6)))*-1)
```

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最高价、最低价、成交量，再用横截面排序、滚动相关把短期 价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**2、20、60、9、6、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha124 - 成交量/资金活跃** 

```
(CLOSE-VWAP)/DECAYLINEAR(RANK(TSMAX(CLOSE,30)),2)
```

**关键拆解：RANK=当日股票池横截面排序；DECAYLINEAR=越近的数据权重越高** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取收盘价、均价/VWAP，再用横截面排序、线性衰减加权把 短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**30、2；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

**Alpha 191 因子公式小白学习手册** 

**48** 

## **Alpha125 - 量价相关/协同** 

```
(RANK(DECAYLINEAR(CORR((VWAP),MEAN(VOLUME,80),17),20))/RANK(DECAYLINEAR(DELTA(((CLOSE*0.5)+
(VWAP*0.5)),3),16)))
```

## **关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；CORR=过去 n** 

**天两个序列的相关系数；DECAYLINEAR=越近的数据权重越高** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取收盘价、均价/VWAP、成交量，再用横截面排序、滚动相关 、变化量、线性衰减加权把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**80、17、20、5、3、16；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha126 - 综合价格形态** 

```
(CLOSE+HIGH+LOW)/3
```

## **关键拆解：主要是价格和成交量字段的四则组合，重点看变量之间的相对位置。** 

**一句话：这是一个偏「综合价格形态」的因子，主要读取最高价、最低价、收盘价，再用四则运算把短期价格/成交行为 压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**3；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha127 - 综合价格形态** 

```
(MEAN((100*(CLOSE-MAX(CLOSE,12))/(MAX(CLOSE,12)))^2))^(1/2)
```

## **关键拆解：^=幂运算，不是异或** 

**一句话：这是一个偏「综合价格形态」的因子，主要读取收盘价，再用幂运算把短期价格/成交行为压缩成一个可排序分 数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**100、12、2、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**49** 

## **Alpha128 - 成交量/资金活跃** 

```
100-(100/(1+SUM(((HIGH+LOW+CLOSE)/3>DELAY((HIGH+LOW+CLOSE)/3,1)?(HIGH+LOW+CLOSE)/3*VOLUME:0),
14)/SUM(((HIGH+LOW+CLOSE)/3<DELAY((HIGH+LOW+CLOSE)/3,1)?(HIGH+LOW+CLOSE)/3*VOLUME:0),14)))
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取最高价、最低价、收盘价、成交量，再用滞后值、条件分支 把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**100、1、3、14；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha129 - 动量反转/均值回复** 

```
SUM((CLOSE-DELAY(CLOSE,1)<0?ABS(CLOSE-DELAY(CLOSE,1)):0),12)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、条件分支把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、12；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它本质上在看短期收益。收益可以被当作动量，也可以被反向使用为均值回复信号。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha130 - 量价相关/协同** 

```
(RANK(DECAYLINEAR(CORR(((HIGH+LOW)/2),MEAN(VOLUME,40),9),10))/
RANK(DECAYLINEAR(CORR(RANK(VWAP),RANK(VOLUME),7),3)))
```

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数；DECAYLINEAR=越近的数据权重越高** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最高价、最低价、均价/VWAP、成交量，再用横截面排序、 滚动相关、线性衰减加权把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**2、40、9、10、7、3；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

**Alpha 191 因子公式小白学习手册** 

**50** 

## **Alpha131 - 量价相关/协同** 

```
(RANK(DELTA(VWAP,1))^TSRANK(CORR(CLOSE,MEAN(VOLUME,50),18),18))
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；TSRANK=单只股票在过去 n 天内的历史排名；CORR=过去 n 天两个序列的相关系数；^=幂运算，不是异或** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取收盘价、均价/VWAP、成交量，再用横截面排序、时间序列 排名、滚动相关、变化量、幂运算把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、50、18；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **TSRANK 是单只股票自己的历史排名，不能和 RANK 混用。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

**校对提示：原文/网页写作 DELAT，本手册按附录函数口径整理为 DELTA。** 

## **Alpha132 - 成交量/资金活跃** 

```
MEAN(AMOUNT,20)
```

## **关键拆解：主要是价格和成交量字段的四则组合，重点看变量之间的相对位置。** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取成交额，再用四则运算把短期价格/成交行为压缩成一个可 排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **计算后建议做去极值、缺失值处理和行业/市值中性化，再检验 IC 与分层收益。** 

## **Alpha133 - 波动振幅/日内结构** 

```
((20-HIGHDAY(HIGH,20))/20)*100-((20-LOWDAY(LOW,20))/20)*100
```

## **关键拆解：HIGHDAY=最高值距离今天几天；LOWDAY=最低值距离今天几天** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取最高价、最低价、方向波动/真实波幅，再用四则运算把 短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**51** 

## **Alpha134 - 成交量/资金活跃** 

```
(CLOSE-DELAY(CLOSE,12))/DELAY(CLOSE,12)*VOLUME
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取收盘价、成交量，再用滞后值把短期价格/成交行为压缩成 一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**12；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它本质上在看短期收益。收益可以被当作动量，也可以被反向使用为均值回复信号。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha135 - 动量反转/均值回复** 

```
SMA(DELAY(CLOSE/DELAY(CLOSE,20),1),20,1)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；SMA=递推平滑均线** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、平滑均线把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

- **新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha136 - 量价相关/协同** 

```
((-1*RANK(DELTA(RET,3)))*CORR(OPEN,VOLUME,10))
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取开盘价、成交量、收益率，再用横截面排序、滚动相关、变化 量把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、3、10；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha137 - 动量反转/均值回复** 

> `16*(CLOSE-DELAY(CLOSE,1)+(CLOSE-OPEN)/2+DELAY(CLOSE,1)-DELAY(OPEN,1))/((ABS(HIGH-DELAY(CLOSE, 1))>ABS(LOW-DELAY(CLOSE,1))&ABS(HIGH-DELAY(CLOSE,1))>ABS(HIGH-DELAY(LOW,1))?ABS(HIGHDELAY(CLOSE,1))+ABS(LOW-DELAY(CLOSE,1))/2+ABS(DELAY(CLOSE,1)-DELAY(OPEN,1))/4:(ABS(LOWDELAY(CLOSE,1))>ABS(HIGH-DELAY(LOW,1))&ABS(LOW-DELAY(CLOSE,1))>ABS(HIGH-DELAY(CLOSE,1))? ABS(LOW-DELAY(CLOSE,1))+ABS(HIGH-DELAY(CLOSE,1))/2+ABS(DELAY(CLOSE,1)-DELAY(OPEN,1))/4:` 

**Alpha 191 因子公式小白学习手册** 

**52** 

```
ABS(HIGH-DELAY(LOW,1))+ABS(DELAY(CLOSE,1)-DELAY(OPEN,1))/4)))*MAX(ABS(HIGH-DELAY(CLOSE,1)),
ABS(LOW-DELAY(CLOSE,1)))
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取开盘价、最高价、最低价、收盘价，再用滞后值、条件分 支把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**16、1、2、4；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它本质上在看短期收益。收益可以被当作动量，也可以被反向使用为均值回复信号。** 

- **注意分母为 0 或极小值时要做保护。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha138 - 量价相关/协同** 

```
((RANK(DECAYLINEAR(DELTA((((LOW*0.7)+(VWAP*0.3))),3),20))-
TSRANK(DECAYLINEAR(TSRANK(CORR(TSRANK(LOW,8),TSRANK(MEAN(VOLUME,60),17),5),19),16),7))*-1)
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；TSRANK=单只股票在过去 n 天内的历史排名；CORR=过去 n 天两个序列的相关系数；DECAYLINEAR=越近的数据权重越高** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最低价、均价/VWAP、成交量，再用横截面排序、时间序列 排名、滚动相关、变化量、线性衰减加权把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为 7、3、20、8、60、17、5、19；再计算公式里 的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **TSRANK 是单只股票自己的历史排名，不能和 RANK 混用。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha139 - 量价相关/协同** 

```
(-1*CORR(OPEN,VOLUME,10))
```

**关键拆解：CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取开盘价、成交量，再用滚动相关把短期价格/成交行为压缩成 一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、10；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

**Alpha 191 因子公式小白学习手册** 

**53** 

## **Alpha140 - 量价相关/协同** 

```
MIN(RANK(DECAYLINEAR(((RANK(OPEN)+RANK(LOW))-(RANK(HIGH)+RANK(CLOSE))),8)),
TSRANK(DECAYLINEAR(CORR(TSRANK(CLOSE,8),TSRANK(MEAN(VOLUME,60),20),8),7),3))
```

**关键拆解：RANK=当日股票池横截面排序；TSRANK=单只股票在过去 n 天内的历史排名；CORR=过去 n 天两个序列的相关系数；DECAYLINEAR=越近的数据权重越高** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取开盘价、最高价、最低价、收盘价、成交量，再用横截面排序 、时间序列排名、滚动相关、线性衰减加权把短期价格/成交行为压缩成一个可排序分数。** 

## **从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**8、60、20、7、3；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **TSRANK 是单只股票自己的历史排名，不能和 RANK 混用。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha141 - 量价相关/协同** 

```
(RANK(CORR(RANK(HIGH),RANK(MEAN(VOLUME,15)),9))*-1)
```

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最高价、成交量，再用横截面排序、滚动相关把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**15、9、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

**Alpha 191 因子公式小白学习手册** 

**54** 

## **Alpha142 - 成交量/资金活跃** 

```
(((-1*RANK(TSRANK(CLOSE,10)))*RANK(DELTA(DELTA(CLOSE,1),1)))*RANK(TSRANK((VOLUME/MEAN(VOLUME,
20)),5)))
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；TSRANK=单只股票在过去 n 天内的历史排名** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取收盘价、成交量，再用横截面排序、时间序列排名、变化量 把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、10、20、5；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **TSRANK 是单只股票自己的历史排名，不能和 RANK 混用。** 

## **Alpha143 - 动量反转/均值回复** 

```
CLOSE>DELAY(CLOSE,1)?(CLOSE-DELAY(CLOSE,1))/DELAY(CLOSE,1)*SELF:SELF
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C；SELF=上一期自身因子值** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、条件分支、递归值把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它本质上在看短期收益。收益可以被当作动量，也可以被反向使用为均值回复信号。** 

- **注意分母为 0 或极小值时要做保护。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

- **SELF 是递归变量，需要先定义初始值。** 

## **Alpha144 - 成交量/资金活跃** 

```
SUMIF(ABS(CLOSE/DELAY(CLOSE,1)-1)/AMOUNT,20,CLOSE<DELAY(CLOSE,1))/COUNT(CLOSE<DELAY(CLOSE,1),
20)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；COUNT=统计条件成立的次数；SUMIF=只对满足条件的样本求和** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取收盘价、成交额，再用滞后值、条件计数、条件求和把短期 价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它像是在数事件：过去若干天里，某种上涨、下跌、放量或同向条件出现了多少次。频率本身就是信号。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**55** 

## **Alpha145 - 成交量/资金活跃** 

```
(MEAN(VOLUME,9)-MEAN(VOLUME,26))/MEAN(VOLUME,12)*100
```

**关键拆解：主要是价格和成交量字段的四则组合，重点看变量之间的相对位置。** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取成交量，再用四则运算把短期价格/成交行为压缩成一个可 排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**9、26、12、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha146 - 动量反转/均值回复** 

```
MEAN((CLOSE-DELAY(CLOSE,1))/DELAY(CLOSE,1)-SMA((CLOSE-DELAY(CLOSE,1))/DELAY(CLOSE,1),61,2),
20)*((CLOSE-DELAY(CLOSE,1))/DELAY(CLOSE,1)-SMA((CLOSE-DELAY(CLOSE,1))/DELAY(CLOSE,1),61,2))/
SMA(((CLOSE-DELAY(CLOSE,1))/DELAY(CLOSE,1)-((CLOSE-DELAY(CLOSE,1))/DELAY(CLOSE,1)-SMA((CLOSE-
DELAY(CLOSE,1))/DELAY(CLOSE,1),61,2)))^2,60)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；SMA=递推平滑均线；^=幂运算，不是异或** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、平滑均线、幂运算把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、61、2、20、60；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha147 - 市场联动/回归** 

```
REGBETA(MEAN(CLOSE,12),SEQUENCE(12))
```

**关键拆解：REGBETA=回归斜率/敏感度** 

**一句话：这是一个偏「市场联动/回归」的因子，主要读取收盘价，再用回归系数把短期价格/成交行为压缩成一个可排 序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**12；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它把个股和市场或风格因子放在一起比较，试图剥离大盘/风格影响后观察个股自身的短期特征。** 

- **计算后建议做去极值、缺失值处理和行业/市值中性化，再检验 IC 与分层收益。** 

**Alpha 191 因子公式小白学习手册** 

**56** 

## **Alpha148 - 量价相关/协同** 

```
((RANK(CORR((OPEN),SUM(MEAN(VOLUME,60),9),6))<RANK((OPEN-TSMIN(OPEN,14))))*-1)
```

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取开盘价、成交量，再用横截面排序、滚动相关把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**60、9、6、14、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha149 - 市场联动/回归** 

```
REGBETA(FILTER(CLOSE/DELAY(CLOSE,1)-1,BANCHMARKINDEXCLOSE<DELAY(BANCHMARKINDEXCLOSE,1)),
FILTER(BANCHMARKINDEXCLOSE/DELAY(BANCHMARKINDEXCLOSE,1)-1,
BANCHMARKINDEXCLOSE<DELAY(BANCHMARKINDEXCLOSE,1)),252)
```

## **关键拆解：DELAY=取 n 日前的值，用来和过去比较；REGBETA=回归斜率/敏感度；FILTER=按条件筛选样本** 

**一句话：这是一个偏「市场联动/回归」的因子，主要读取收盘价、基准指数，再用滞后值、回归系数、条件筛选把短期 价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、252；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它把个股和市场或风格因子放在一起比较，试图剥离大盘/风格影响后观察个股自身的短期特征。** 

- **注意分母为 0 或极小值时要做保护。** 

**校对提示：BANCHMARK 是报告原文拼法，含义为 BENCHMARK，即基准指数。** 

## **Alpha150 - 成交量/资金活跃** 

```
(CLOSE+HIGH+LOW)/3*VOLUME
```

## **关键拆解：主要是价格和成交量字段的四则组合，重点看变量之间的相对位置。** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取最高价、最低价、收盘价、成交量，再用四则运算把短期价 格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**3；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**57** 

## **Alpha151 - 动量反转/均值回复** 

```
SMA(CLOSE-DELAY(CLOSE,20),20,1)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；SMA=递推平滑均线** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、平滑均线把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha152 - 动量反转/均值回复** 

```
SMA(MEAN(DELAY(SMA(DELAY(CLOSE/DELAY(CLOSE,9),1),9,1),1),12)-MEAN(DELAY(SMA(DELAY(CLOSE/
DELAY(CLOSE,9),1),9,1),1),26),9,1)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；SMA=递推平滑均线** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、平滑均线把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**9、1、12、26；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha153 - 动量反转/均值回复** 

```
(MEAN(CLOSE,3)+MEAN(CLOSE,6)+MEAN(CLOSE,12)+MEAN(CLOSE,24))/4
```

**关键拆解：主要是价格和成交量字段的四则组合，重点看变量之间的相对位置。** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用四则运算把短期价格/成交行为压缩成一个 可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**3、6、12、24、4；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**58** 

## **Alpha154 - 量价相关/协同** 

```
(((VWAP-MIN(VWAP,16)))<(CORR(VWAP,MEAN(VOLUME,180),18)))
```

**关键拆解：CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取均价/VWAP、成交量，再用滚动相关把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**16、180、18；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha155 - 成交量/资金活跃** 

```
SMA(VOLUME,13,2)-SMA(VOLUME,27,2)-SMA(SMA(VOLUME,13,2)-SMA(VOLUME,27,2),10,2)
```

**关键拆解：SMA=递推平滑均线** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取成交量，再用平滑均线把短期价格/成交行为压缩成一个可 排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**13、2、27、10；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha156 - 成交量/资金活跃** 

- `(MAX(RANK(DECAYLINEAR(DELTA(VWAP,5),3)),RANK(DECAYLINEAR(((DELTA(((OPEN*0.15)+(LOW*0.85)),2)/ ((OPEN*0.15)+(LOW*0.85)))*-1),3)))*-1)` 

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；DECAYLINEAR=越近的数据权重越高** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取开盘价、最低价、均价/VWAP，再用横截面排序、变化量 、线性衰减加权把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **5、3、15、85、2、1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

## **高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

**Alpha 191 因子公式小白学习手册** 

**59** 

## **Alpha157 - 排序位置/相对强弱** 

```
(MIN(PROD(RANK(RANK(LOG(SUM(TSMIN(RANK(RANK((-1*RANK(DELTA((CLOSE-1),5))))),2),1)))),1),5)+
TSRANK(DELAY((-1*RET),6),5))
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；DELTA=当前值减 n** 

**日前值，表示变化量；RANK=当日股票池横截面排序；TSRANK=单只股票在过去 n 天内的历史排名** 

**一句话：这是一个偏「排序位置/相对强弱」的因子，主要读取收盘价、收益率，再用横截面排序、时间序列排名、变化 量、滞后值把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、5、2、6；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **TSRANK 是单只股票自己的历史排名，不能和 RANK 混用。** 

## **Alpha158 - 动量反转/均值回复** 

```
((HIGH-SMA(CLOSE,15,2))-(LOW-SMA(CLOSE,15,2)))/CLOSE
```

**关键拆解：SMA=递推平滑均线** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取最高价、最低价、收盘价，再用平滑均线把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**15、2；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha159 - 动量反转/均值回复** 

```
((CLOSE-SUM(MIN(LOW,DELAY(CLOSE,1)),6))/SUM(MAX(HIGH,DELAY(CLOSE,1))-MIN(LOW,DELAY(CLOSE,1)),
6)*12*24+(CLOSE-SUM(MIN(LOW,DELAY(CLOSE,1)),12))/SUM(MAX(HIGH,DELAY(CLOSE,1))-MIN(LOW,
DELAY(CLOSE,1)),12)*6*24+(CLOSE-SUM(MIN(LOW,DELAY(CLOSE,1)),24))/SUM(MAX(HIGH,DELAY(CLOSE,1))
-MIN(LOW,DELAY(CLOSE,1)),24)*6*24)*100/(6*12+6*24+12*24)
```

## **关键拆解：DELAY=取 n 日前的值，用来和过去比较** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取最高价、最低价、收盘价，再用滞后值把短期价格/成交 行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、6、12、24、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

**校对提示：原文/网页中 HGIH 应为 HIGH，本手册按价格字段口径整理。** 

**Alpha 191 因子公式小白学习手册** 

**60** 

## **Alpha160 - 波动振幅/日内结构** 

```
SMA((CLOSE<=DELAY(CLOSE,1)?STD(CLOSE,20):0),20,1)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；STD=过去 n 天标准差，衡量波动；SMA=递推平滑均线；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取收盘价，再用滞后值、平滑均线、波动率、条件分支把短 期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放** 

**。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha161 - 波动振幅/日内结构** 

```
MEAN(MAX(MAX((HIGH-LOW),ABS(DELAY(CLOSE,1)-HIGH)),ABS(DELAY(CLOSE,1)-LOW)),12)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取最高价、最低价、收盘价，再用滞后值把短期价格/成交 行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、12；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **计算后建议做去极值、缺失值处理和行业/市值中性化，再检验 IC 与分层收益。** 

## **Alpha162 - 动量反转/均值回复** 

```
(SMA(MAX(CLOSE-DELAY(CLOSE,1),0),12,1)/SMA(ABS(CLOSE-DELAY(CLOSE,1)),12,1)*100-
MIN(SMA(MAX(CLOSE-DELAY(CLOSE,1),0),12,1)/SMA(ABS(CLOSE-DELAY(CLOSE,1)),12,1)*100,12))/
(MAX(SMA(MAX(CLOSE-DELAY(CLOSE,1),0),12,1)/SMA(ABS(CLOSE-DELAY(CLOSE,1)),12,1)*100,12)-
MIN(SMA(MAX(CLOSE-DELAY(CLOSE,1),0),12,1)/SMA(ABS(CLOSE-DELAY(CLOSE,1)),12,1)*100,12))
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；SMA=递推平滑均线** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、平滑均线把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、12、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

**Alpha 191 因子公式小白学习手册** 

**61** 

## **Alpha163 - 成交量/资金活跃** 

```
RANK(((((-1*RET)*MEAN(VOLUME,20))*VWAP)*(HIGH-CLOSE)))
```

**关键拆解：RANK=当日股票池横截面排序** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取最高价、收盘价、均价/VWAP、成交量、收益率，再用横 截面排序把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

## **Alpha164 - 波动振幅/日内结构** 

```
SMA((((CLOSE>DELAY(CLOSE,1))?1/(CLOSE-DELAY(CLOSE,1)):1)-MIN(((CLOSE>DELAY(CLOSE,1))?1/
(CLOSE-DELAY(CLOSE,1)):1),12))/(HIGH-LOW)*100,13,2)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；SMA=递推平滑均线；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取最高价、最低价、收盘价，再用滞后值、平滑均线、条件 分支把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、12、100、13、2；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放** 

**。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha165 - 波动振幅/日内结构** 

```
MAX(SUMAC(CLOSE-MEAN(CLOSE,48)))-MIN(SUMAC(CLOSE-MEAN(CLOSE,48)))/STD(CLOSE,48)
```

**关键拆解：STD=过去 n 天标准差，衡量波动** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取收盘价，再用波动率把短期价格/成交行为压缩成一个可 排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**48；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**62** 

## **Alpha166 - 动量反转/均值回复** 

`-20*` I `20-1` I `^1.5*SUM(CLOSE/DELAY(CLOSE,1)-1-MEAN(CLOSE/DELAY(CLOSE,1)-1,20),20)/((20-1)*(20-2) (SUM((CLOSE/DELAY(CLOSE,1),20)^2,20))^1.5)` 

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；^=幂运算，不是异或** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、幂运算把短期价格/成交行为压缩 成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20、1、5、2；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它本质上在看短期收益。收益可以被当作动量，也可以被反向使用为均值回复信号。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha167 - 动量反转/均值回复** 

```
SUM((CLOSE-DELAY(CLOSE,1)>0?CLOSE-DELAY(CLOSE,1):0),12)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、条件分支把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、12；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它本质上在看短期收益。收益可以被当作动量，也可以被反向使用为均值回复信号。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha168 - 成交量/资金活跃** 

```
(-1*VOLUME/MEAN(VOLUME,20))
```

## **关键拆解：主要是价格和成交量字段的四则组合，重点看变量之间的相对位置。** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取成交量，再用四则运算把短期价格/成交行为压缩成一个可 排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式中含有 -1** 

**反向处理：原始特征越强，最终因子值往往越低；排序选股时要按回测方向决定取高值还是低值。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**63** 

## **Alpha169 - 动量反转/均值回复** 

```
SMA(MEAN(DELAY(SMA(CLOSE-DELAY(CLOSE,1),9,1),1),12)-MEAN(DELAY(SMA(CLOSE-DELAY(CLOSE,1),9,1),
1),26),10,1)
```

## **关键拆解：DELAY=取 n 日前的值，用来和过去比较；SMA=递推平滑均线** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用滞后值、平滑均线把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、9、12、26、10；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

- **新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha170 - 成交量/资金活跃** 

```
((((RANK((1/CLOSE))*VOLUME)/MEAN(VOLUME,20))*((HIGH*RANK((HIGH-CLOSE)))/(SUM(HIGH,5)/5)))-
RANK((VWAP-DELAY(VWAP,5))))
```

## **关键拆解：DELAY=取 n 日前的值，用来和过去比较；RANK=当日股票池横截面排序** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取最高价、收盘价、均价/VWAP、成交量，再用横截面排序 、滞后值把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、20、5；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

## **Alpha171 - 综合价格形态** 

```
((-1*((LOW-CLOSE)*(OPEN^5)))/((CLOSE-HIGH)*(CLOSE^5)))
```

## **关键拆解：^=幂运算，不是异或** 

**一句话：这是一个偏「综合价格形态」的因子，主要读取开盘价、最高价、最低价、收盘价，再用幂运算把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、5；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**64** 

## **Alpha172 - 波动振幅/日内结构** 

```
MEAN(ABS(SUM((LD>0&LD>HD)?LD:0,14)*100/SUM(TR,14)-SUM((HD>0&HD>LD)?HD:0,14)*100/SUM(TR,14))/
(SUM((LD>0&LD>HD)?LD:0,14)*100/SUM(TR,14)+SUM((HD>0&HD>LD)?HD:0,14)*100/SUM(TR,14))*100,6)
```

## **关键拆解：A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取方向波动/真实波幅，再用条件分支把短期价格/成交行为 压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**14、100、6；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **注意分母为 0 或极小值时要做保护。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha173 - 动量反转/均值回复** 

```
3*SMA(CLOSE,13,2)-2*SMA(SMA(CLOSE,13,2),13,2)+SMA(SMA(SMA(LOG(CLOSE),13,2),13,2),13,2)
```

**关键拆解：SMA=递推平滑均线** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用平滑均线把短期价格/成交行为压缩成一个 可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**3、13、2；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它先把噪声较大的日频数据平滑，再看趋势是否延续或偏离。平滑不是预测魔法，只是让短期信号更稳定。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

## **Alpha174 - 波动振幅/日内结构** 

```
SMA((CLOSE>DELAY(CLOSE,1)?STD(CLOSE,20):0),20,1)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；STD=过去 n 天标准差，衡量波动；SMA=递推平滑均线；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取收盘价，再用滞后值、平滑均线、波动率、条件分支把短 期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

**Alpha 191 因子公式小白学习手册** 

**65** 

## **Alpha175 - 波动振幅/日内结构** 

```
MEAN(MAX(MAX((HIGH-LOW),ABS(DELAY(CLOSE,1)-HIGH)),ABS(DELAY(CLOSE,1)-LOW)),6)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取最高价、最低价、收盘价，再用滞后值把短期价格/成交 行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、6；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **计算后建议做去极值、缺失值处理和行业/市值中性化，再检验 IC 与分层收益。** 

## **Alpha176 - 量价相关/协同** 

```
CORR(RANK(((CLOSE-TSMIN(LOW,12))/(TSMAX(HIGH,12)-TSMIN(LOW,12)))),RANK(VOLUME),6)
```

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最高价、最低价、收盘价、成交量，再用横截面排序、滚动相 关把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**12、6；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha177 - 波动振幅/日内结构** 

```
((20-HIGHDAY(HIGH,20))/20)*100
```

**关键拆解：HIGHDAY=最高值距离今天几天** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取最高价、方向波动/真实波幅，再用四则运算把短期价格/ 成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**66** 

## **Alpha178 - 成交量/资金活跃** 

```
(CLOSE-DELAY(CLOSE,1))/DELAY(CLOSE,1)*VOLUME
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较** 

**一句话：这是一个偏「成交量/资金活跃」的因子，主要读取收盘价、成交量，再用滞后值把短期价格/成交行为压缩成 一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它本质上在看短期收益。收益可以被当作动量，也可以被反向使用为均值回复信号。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha179 - 量价相关/协同** 

```
(RANK(CORR(VWAP,VOLUME,4))*RANK(CORR(RANK(LOW),RANK(MEAN(VOLUME,50)),12)))
```

**关键拆解：RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最低价、均价/VWAP、成交量，再用横截面排序、滚动相关 把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**4、50、12；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha180 - 成交量/资金活跃** 

```
((MEAN(VOLUME,20)<VOLUME)?((-1*TSRANK(ABS(DELTA(CLOSE,7)),60))*SIGN(DELTA(CLOSE,7)):(-1*
VOLUME)))
```

**关键拆解：DELTA=当前值减 n 日前值，表示变化量；RANK=当日股票池横截面排序；TSRANK=单只股票在过去 n 天内的历史排名；A?B:C=如果 A 成立取 B，否则取 C** 

- **一句话：这是一个偏「成交量/资金活跃」的因子，主要读取收盘价、成交量，再用横截面排序、时间序列排名、变化量 、条件分支把短期价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20、1、7、60；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **TSRANK 是单只股票自己的历史排名，不能和 RANK 混用。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

**Alpha 191 因子公式小白学习手册** 

**67** 

## **Alpha181 - 市场联动/回归** 

```
SUM(((CLOSE/DELAY(CLOSE,1)-1)-MEAN((CLOSE/DELAY(CLOSE,1)-1),20))-(BANCHMARKINDEXCLOSE-
MEAN(BANCHMARKINDEXCLOSE,20))^2,20)/SUM((BANCHMARKINDEXCLOSE-MEAN(BANCHMARKINDEXCLOSE,20))^3)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；^=幂运算，不是异或** 

**一句话：这是一个偏「市场联动/回归」的因子，主要读取收盘价、基准指数，再用滞后值、幂运算把短期价格/成交行 为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、20、2、3；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它把个股和市场或风格因子放在一起比较，试图剥离大盘/风格影响后观察个股自身的短期特征。** 

- **注意分母为 0 或极小值时要做保护。** 

**校对提示：BANCHMARK 是报告原文拼法，含义为 BENCHMARK，即基准指数。** 

## **Alpha182 - 市场联动/回归** 

```
COUNT((CLOSE>OPEN&BANCHMARKINDEXCLOSE>BANCHMARKINDEXOPEN)
OR(CLOSE<OPEN&BANCHMARKINDEXCLOSE<BANCHMARKINDEXOPEN),20)/20
```

**关键拆解：COUNT=统计条件成立的次数** 

**一句话：这是一个偏「市场联动/回归」的因子，主要读取开盘价、收盘价、基准指数，再用条件计数把短期价格/成交 行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它像是在数事件：过去若干天里，某种上涨、下跌、放量或同向条件出现了多少次。频率本身就是信号。** 

- **注意分母为 0 或极小值时要做保护。** 

## **Alpha183 - 波动振幅/日内结构** 

```
MAX(SUMAC(CLOSE-MEAN(CLOSE,24)))-MIN(SUMAC(CLOSE-MEAN(CLOSE,24)))/STD(CLOSE,24)
```

**关键拆解：STD=过去 n 天标准差，衡量波动** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取收盘价，再用波动率把短期价格/成交行为压缩成一个可 排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**24；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **注意分母为 0 或极小值时要做保护。** 

**Alpha 191 因子公式小白学习手册** 

**68** 

## **Alpha184 - 量价相关/协同** 

```
(RANK(CORR(DELAY((OPEN-CLOSE),1),CLOSE,200))+RANK((OPEN-CLOSE)))
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；RANK=当日股票池横截面排序；CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取开盘价、收盘价，再用横截面排序、滚动相关、滞后值把短期 价格/成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、200；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

## **Alpha185 - 排序位置/相对强弱** 

```
RANK((-1*((1-(OPEN/CLOSE))^2)))
```

**关键拆解：RANK=当日股票池横截面排序；^=幂运算，不是异或** 

**一句话：这是一个偏「排序位置/相对强弱」的因子，主要读取开盘价、收盘价，再用横截面排序、幂运算把短期价格/ 成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**1、2；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含排序：数值越大通常表示该特征在截面或时间窗口中越靠前，但是否代表买入信号仍要看实证 方向。** 

**新手直觉：它强调相对位置：不是问这只股票绝对涨了多少，而是问它在全部股票或自己历史窗口中排第几。** 

- **注意分母为 0 或极小值时要做保护。** 

- **RANK 通常是当日横截面排序，股票池不同会改变数值。** 

## **Alpha186 - 波动振幅/日内结构** 

```
(MEAN(ABS(SUM((LD>0&LD>HD)?LD:0,14)*100/SUM(TR,14)-SUM((HD>0&HD>LD)?HD:0,14)*100/SUM(TR,14))/
(SUM((LD>0&LD>HD)?LD:0,14)*100/SUM(TR,14)+SUM((HD>0&HD>LD)?HD:0,14)*100/SUM(TR,14))*100,6)+
DELAY(MEAN(ABS(SUM((LD>0&LD>HD)?LD:0,14)*100/SUM(TR,14)-SUM((HD>0&HD>LD)?HD:0,14)*100/SUM(TR,
14))/(SUM((LD>0&LD>HD)?LD:0,14)*100/SUM(TR,14)+SUM((HD>0&HD>LD)?HD:0,14)*100/SUM(TR,14))*100,
6),6))/2
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取方向波动/真实波幅，再用滞后值、条件分支把短期价格/ 成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**14、100、6、2；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放 。** 

- **注意分母为 0 或极小值时要做保护。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

**Alpha 191 因子公式小白学习手册** 

**69** 

## **Alpha187 - 条件统计/规则触发** 

```
SUM((OPEN<=DELAY(OPEN,1)?0:MAX((HIGH-OPEN),(OPEN-DELAY(OPEN,1)))),20)
```

**关键拆解：DELAY=取 n 日前的值，用来和过去比较；A?B:C=如果 A 成立取 B，否则取 C** 

**一句话：这是一个偏「条件统计/规则触发」的因子，主要读取开盘价、最高价，再用滞后值、条件分支把短期价格/成 交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

- **1、20；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：公式含条件分支：不同市场状态下会切换计算方式，因此高低值要结合触发条件理解。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **条件表达式 A?B:C 表示 if A then B else C。** 

## **Alpha188 - 波动振幅/日内结构** 

```
((HIGH-LOW-SMA(HIGH-LOW,11,2))/SMA(HIGH-LOW,11,2))*100
```

**关键拆解：SMA=递推平滑均线** 

**一句话：这是一个偏「波动振幅/日内结构」的因子，主要读取最高价、最低价，再用平滑均线把短期价格/成交行为压 缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**11、2、100；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它关注的是波动和振幅。波动突然变大、变小，或波动集中在上影/下影，常被用来识别情绪强弱与风险释放** 

**。** 

- **注意分母为 0 或极小值时要做保护。** 

- **平滑函数在不同平台的初值处理可能不同，复现时要固定口径。** 

- **校对提示：原文抽取中横线字符混杂，本手册统一为减号。** 

## **Alpha189 - 动量反转/均值回复** 

```
MEAN(ABS(CLOSE-MEAN(CLOSE,6)),6)
```

**关键拆解：主要是价格和成交量字段的四则组合，重点看变量之间的相对位置。** 

**一句话：这是一个偏「动量反转/均值回复」的因子，主要读取收盘价，再用四则运算把短期价格/成交行为压缩成一个 可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**6；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：它是由几个基础价量字段组合出来的形态指标，核心是把肉眼看图的经验转成稳定、可批量计算的数字。** 

- **计算后建议做去极值、缺失值处理和行业/市值中性化，再检验 IC 与分层收益。** 

## **Alpha190 - 动量反转/均值回复** 

```
LOG((COUNT(CLOSE/DELAY(CLOSE)-1>((CLOSE/DELAY(CLOSE,19))^(1/20)-1),20)-1)*(SUMIF(((CLOSE/
DELAY(CLOSE)-1-(CLOSE/DELAY(CLOSE,19))^(1/20)-1))^2,20,CLOSE/DELAY(CLOSE)-1<(CLOSE/
DELAY(CLOSE,19))^(1/20)-1))/((COUNT((CLOSE/DELAY(CLOSE)-1<(CLOSE/DELAY(CLOSE,19))^(1/20)-1),
20))*(SUMIF((CLOSE/DELAY(CLOSE)-1-((CLOSE/DELAY(CLOSE,19))^(1/20)-1))^2,20,CLOSE/DELAY(CLOSE)
-1>(CLOSE/DELAY(CLOSE,19))^(1/20)-1))))
```

## **Alpha191 - 量价相关/协同** 

```
((CORR(MEAN(VOLUME,20),LOW,5)+((HIGH+LOW)/2))-CLOSE)
```

## **关键拆解：CORR=过去 n 天两个序列的相关系数** 

**一句话：这是一个偏「量价相关/协同」的因子，主要读取最高价、最低价、收盘价、成交量，再用滚动相关把短期价格 /成交行为压缩成一个可排序分数。** 

**从里到外：从里到外读：先准备原始序列和滞后/窗口数据，主要窗口为** 

**20、5、2；再计算公式里的变化、相关、平滑或条件统计；最后用排序、乘号或正负号得到当日因子值。** 

**高低值怎么理解：没有明显反向符号：高值通常表示该原始特征更强，但实盘使用仍应以分层回测或 IC 方向为准。** 

**新手直觉：小白可以把它想成“价格动作和成交动作是否同步”。同步、背离、突然转向，都可能暴露短期交易资金的 行为。** 

- **注意分母为 0 或极小值时要做保护。** 

- **相关/协方差至少需要足够窗口样本，停牌或缺失值要统一处理。** 

**Alpha 191 因子公式小白学习手册** 

**71** 

## **5. 复现与学习建议** 

**Alpha191 更像一个“价量特征库”，不是一套可以直接照抄的投资系统。真正使用时，建议按以下顺序做实验。** 

- **先固定数据口径：复权方式、停牌处理、涨跌停、成交量/成交额单位、指数字段。** 

- **逐个因子做缺失值、极值和方向检查，确认公式在你的平台上没有解释歧义。** 

- **做单因子分层、IC/RankIC、换手率和交易成本敏感性，不要只看收益曲线。** 

- **把相关性很高的因子合并或降权，避免 191 个公式重复表达同一个短周期特征。** 

- **组合层面控制行业、市值、流动性等暴露，再评估是否真的来自价量 Alpha。** 

## **按类别索引** 

|**类别**|**因子编号**|
|---|---|
|**动量反转/均值回复**|**Alpha003、Alpha014、Alpha015、Alpha018、Alpha019、Alpha020、Alpha022、Alpha024**<br>**、Alpha027 Alpha031、Alpha034、Alpha046、Alpha053、Alpha055、Alpha058、Alpha059**<br>**、Alpha063、Alpha065 Alpha066、Alpha067、Alpha071、Alpha079、Alpha086、Alpha088**<br>**、Alpha089、Alpha098、Alpha106 Alpha110、Alpha112、Alpha129、Alpha135、Alpha137**<br>**、Alpha143、Alpha146、Alpha151、Alpha152 Alpha153、Alpha158、Alpha159、Alpha162**<br>**、Alpha166、Alpha167、Alpha169、Alpha173、Alpha189 Alpha190**|
|**市场联动/回归**|**Alpha021、Alpha030、Alpha075、Alpha116、Alpha147、Alpha149、Alpha181、Alpha182**|
|**成交量/资金活跃**|**Alpha007、Alpha008、Alpha012、Alpha013、Alpha017、Alpha025、Alpha029、Alpha033**<br>**、Alpha040 Alpha041、Alpha043、Alpha048、Alpha080、Alpha081、Alpha084、Alpha085**<br>**、Alpha087、Alpha094 Alpha102、Alpha117、Alpha120、Alpha124、Alpha128、Alpha132**<br>**、Alpha134、Alpha142、Alpha144 Alpha145、Alpha150、Alpha155、Alpha156、Alpha163**<br>**、Alpha168、Alpha170、Alpha178、Alpha180**|
|**排序位置/相对强弱**|**Alpha006、Alpha037、Alpha103、Alpha107、Alpha157、Alpha185**|
|**条件统计/规则触发**|**Alpha038、Alpha049、Alpha050、Alpha051、Alpha093、Alpha187**|
|**波动振幅/日内结构**|**Alpha002、Alpha004、Alpha009、Alpha010、Alpha011、Alpha023、Alpha060、Alpha068**<br>**、Alpha069 Alpha070、Alpha076、Alpha095、Alpha097、Alpha100、Alpha109、Alpha111**<br>**、Alpha114、Alpha133 Alpha160、Alpha161、Alpha164、Alpha165、Alpha172、Alpha174**<br>**、Alpha175、Alpha177、Alpha183 Alpha186、Alpha188**|
|**综合价格形态**|**Alpha028、Alpha047、Alpha052、Alpha057、Alpha072、Alpha078、Alpha082、Alpha096**<br>**、Alpha118 Alpha122、Alpha126、Alpha127、Alpha171**|
|**量价相关/协同**|**Alpha001、Alpha005、Alpha016、Alpha026、Alpha032、Alpha035、Alpha036、Alpha039**<br>**、Alpha042 Alpha044、Alpha045、Alpha054、Alpha056、Alpha061、Alpha062、Alpha064**<br>**、Alpha073、Alpha074 Alpha077、Alpha083、Alpha090、Alpha091、Alpha092、Alpha099**<br>**、Alpha101、Alpha104、Alpha105 Alpha108、Alpha113、Alpha115、Alpha119、Alpha121**<br>**、Alpha123、Alpha125、Alpha130、Alpha131 Alpha136、Alpha138、Alpha139、Alpha140**<br>**、Alpha141、Alpha148、Alpha154、Alpha176、Alpha179 Alpha184、Alpha191**|
